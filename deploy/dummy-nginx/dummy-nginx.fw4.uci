#!/bin/sh
# GENERATED by scripts/profile-to-fw4.py from deploy/dummy-nginx/profile.yaml -- DO NOT EDIT BY HAND.
# fw4 rules for the payload's docker bridge (design B, docs/design/ots-networking.md).
# Idempotent; install as a uci-default or source from run.sh. Requires dockerd iptables=0.
set -e
for s in dummy_nginx_zone dummy_nginx_allow dummy_nginx_dnat_0; do uci -q delete firewall."$s" || true; done
uci set firewall.dummy_nginx_zone=zone
uci set firewall.dummy_nginx_zone.name='dummyz'
uci set firewall.dummy_nginx_zone.device='br-dummy'
uci set firewall.dummy_nginx_zone.input='DROP'
uci set firewall.dummy_nginx_zone.output='ACCEPT'
uci set firewall.dummy_nginx_zone.forward='ACCEPT'
uci set firewall.dummy_nginx_zone.masq='0'
uci set firewall.dummy_nginx_dnat_0=redirect
uci set firewall.dummy_nginx_dnat_0.name='dummy-nginx-pub-8090'
uci set firewall.dummy_nginx_dnat_0.src='ahwlan'
uci set firewall.dummy_nginx_dnat_0.src_dport='8090'
uci set firewall.dummy_nginx_dnat_0.dest='dummyz'
uci set firewall.dummy_nginx_dnat_0.dest_ip='172.20.1.10'
uci set firewall.dummy_nginx_dnat_0.dest_port='8080'
uci set firewall.dummy_nginx_dnat_0.proto='tcp'
uci set firewall.dummy_nginx_dnat_0.target='DNAT'
uci set firewall.dummy_nginx_allow=rule
uci set firewall.dummy_nginx_allow.name='dummy-nginx-pub-allow'
uci set firewall.dummy_nginx_allow.src='ahwlan'
uci set firewall.dummy_nginx_allow.dest='dummyz'
uci set firewall.dummy_nginx_allow.dest_ip='172.20.1.10'
uci set firewall.dummy_nginx_allow.proto='tcp'
uci set firewall.dummy_nginx_allow.dest_port='8080'
uci set firewall.dummy_nginx_allow.target='ACCEPT'
uci commit firewall
fw4 reload >/dev/null 2>&1 || true
echo "fw4: dummy-nginx zone dummyz on br-dummy applied"
