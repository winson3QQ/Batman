# GENERATED by scripts/profile-to-flags.py from deploy/dummy-nginx/profile.yaml (values) -- DO NOT EDIT BY HAND.
# Regenerate after any profile change; CI gate asserts this matches (scripts/check-hardening-env.sh).
# run.sh sources this and passes $HARDEN_FLAGS to `docker run`.
HARDEN_FLAGS="--user 101 --security-opt no-new-privileges --cap-drop=ALL --read-only --tmpfs /tmp:rw,nosuid,nodev,size=16m --tmpfs /var/cache/nginx:rw,nosuid,nodev,size=16m --tmpfs /var/run:rw,nosuid,nodev,size=1m --cpus 0.5 --pids-limit 64 --memory 64m"
